# Calor o frio.

¿Te ha pasado que vez una temperatura y crees que está haciendo mucho calor, pero resulta que estaba expresada en grados Fahrenheit (°F) y no en Centígrados (°C)?
Para no seguir teniendo esa confusión, lo mejor será que hagas un programa para hacer la conversión.

Recuerda que la fórmula es:
$$ °C = \frac{5}{9} (°F − 32)$$
#### Entrada:
La entrada contiene una única línea con un valor real (expresado en °F).
#### Salida:
El mensaje (sin comillas ni tildes) 'x grados centigrados', donde x corresponde a la equivalencia.


| Ej. de entrada | Ej. de salida          |
| -------------- | ---------------------- |
| 41.0           | 5.0 grados centigrados |


```python
def conversor_temperatura(farenheit):
    # Escriba su código a continuación
    pass
```

```python
conversor_temperatura(41.0)
```
